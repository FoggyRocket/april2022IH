<h1>Lista de usuarios 🚀</h1>
{{!-- veremos si contemos usuarios --}}
{{#if users}}
    <ul>
        {{!-- creamos una lista dinamica con los usuarios --}}
        {{#each users}}
        <li>
            {{username}} 
            <a href="/update/{{_id}}"> Editar</a>
            <a href="/delete/{{_id}}"> Eliminar</a>
        </li>
        {{/each}}
    </ul>
{{!-- de lo contrario mostramos un mensaje  --}}
{{else}}
    <p>No tienes usuarios aun</p>
{{/if}}

<a href="/create">Crear nuevo usuario</a>